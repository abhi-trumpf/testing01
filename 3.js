const args = process.argv.slice(2);   // going to take inputs 1 by 1    pas 
console.log("Input received from terminal:", args); // ADD THIS LINE

const numArray = args.map(Number); // traverse 1 by 1 and Number does the conversion.
console.log("Converted to numbers:", numArray);

const poll = {
    question : 'What is your fav language'?,
    option : ['0 :Javascript','1:c#','2 :nothing'].
    answers:[0,]
}

