rough


a)Completed 4 problems 
Remove duplicates
move zeros towards the end.
Check if array is sorted
Merge two arrays

Hidden Compilation: Yes. It takes your .py file and turns it into bytecode (the .pyc files you sometimes see).

The Interpreter (The Engine): Yes. CPython is the program that "reads" that bytecode.

The PVM: Yes. The Python Virtual Machine is the part of CPython that acts as the "brain," processing those bytecode instructions 1-by-1.

Written in C: Exactly. Because C is very close to the "metal" of the computer (the hardware), writing the interpreter in C makes Python fast enough to be useful while keeping the syntax easy for us humans.


Since you’ve been working with C#, there is actually a version of the Python interpreter called IronPython. It’s written in C# and allows Python code to interact directly with .NET libraries.





Chapter 5 - Sheet Metal
parts of Sheet Metal - ram drive ,machine drive hydraulic punching head and tool adapter
Removing the parts
Tools and workpiece 
Machine Costs




Part Name,What it does
Punching Head,The overall container for the punching mechanism.
Hydraulic Drive,The power source (Machine Drive) that generates the force.
Ram,The moving part that transmits that force to the tool.
Tool Adapter,The part that physically holds and clicks the tool into place.







using System;


namespace CodingPractise.CsharpCoding
{
    public class SwapNumbers
    {
        public static void Run()
        {
            Console.WriteLine("enter n1");
            int n1 = int.Parse(Console.ReadLine());
            Console.WriteLine("enter n2");
            int n2 = int.Parse(Console.ReadLine());
            Console.WriteLine($"Values before swapping are {n1} and {n2}\n");
            n1 = n2;
            Console.WriteLine($"Values after swapping are {n1} and {n2}\n");
        }
    }
}





using System;

namespace CodingPractise.CsharpCoding
{
    public class SwapNumbers
    {
        public static void Run()
        {
            Console.WriteLine("Enter n1:");
            int n1 = int.Parse(Console.ReadLine() ?? "0");

            Console.WriteLine("Enter n2:");
            int n2 = int.Parse(Console.ReadLine() ?? "0");

            Console.WriteLine($"Values before swapping are: {n1} and {n2}");

            // The actual swap logic
            int temp = n1;
            n1 = n2;
            n2 = temp;

            Console.WriteLine($"Values after swapping are: {n1} and {n2}");
        }
    }
}


C:\NewProject\CodingPractise\CodingPractise\CsharpCoding

file path


moved past simple logic and into the territory of **Software Architecture** and **Professional Environment Management.
### **1. Structural Organization (The "Toolbox" Pattern)**

* **Decoupling Logic:** You moved away from putting all your code in one place. Instead, you learned to wrap specific tasks (like `SwapNumbers` or `ReverseNumber`) into their own dedicated **Classes**.
* **Static Method Invocation:** You used `public static void Run()` to make your code accessible without needing to create new object instances every time, which is a key step in understanding memory and access modifiers.
* **Namespace Management:** You learned how to organize these classes under a single **Namespace** (`CodingPractise.CsharpCoding`) so they can "see" each other and work together.

### **2. The Entry Point Concept**

* **Entry Point Resolution:** You solved the **CS5001** error by understanding that a program needs one (and only one) "Leader" file—your `Program.cs`— to call your other classes. 4. Automated Project Cleanup & CI/CD Readiness
Workspace Lock Resolution: Learned that TestComplete locks certain local log and configuration files (like .tcLS or _backup folders) during execution, which can cause permission conflicts if tracked.

Automation .gitignore Configuration: Discovered the professional way to keep the GitHub repository clean by explicitly configuring Git to ignore local user settings, temporary test logs, and backup files, ensuring seamless team collaboration.
* **Top-Level Statements:** You learned that code living outside of a class is a special "Top-Level" instruction that tells the computer where to start.

### **3. Advanced Input Safety**

* **The Null-Coalescing Operator (`??`):** You learned to use the "Safety Net" (`?? "0"`) to prevent **NullReferenceExceptions** when `Console.ReadLine()` returns nothing.
* **Input Validation Logic:** You explored the difference between simply parsing data and using `int.TryParse` to check if input is "garbage" before the program tries to process it.

### 4. Automated Project Cleanup & CI/CD Readiness
Workspace Lock Resolution: Learned that TestComplete locks certain local log and configuration files (like .tcLS or _backup folders) during execution, which can cause permission conflicts if tracked.(the main goal of Workspace Lock Resolution isn't about "security" in terms of hacking or encryption—it's about Stability and Reliability.
It protects your data in a different way: by preventing corrupted saves and workflow crashes.)


Automation .gitignore Configuration: Discovered the professional way to keep the GitHub repository clean by explicitly configuring Git to ignore local user settings, temporary test logs, and backup files, ensuring sooth transitions.(Basically track only important concepts)
















Modular Scripting Architecture: Transitioned from linear recordings to a modular "Script Unit" approach, allowing common actions to be reused across multiple test cases.

Dynamic Object Handling: Implemented logic to interact with UI elements based on properties rather than hardcoded positions, significantly increasing test stability across different screen resolutions.

Exception & Error Management: Developed "Safety Net" logic using conditional property checks (like .Exists) to prevent test crashes when UI elements are slow to load.(built-in property used to check whether a specific user interface (UI) element (like a button, a window, or a text box) is actually present on the screen).

Runtime Environment Stability: Resolved persistent IDE permission conflicts by identifying and "ignoring" TestComplete’s temporary workspace locks (.tcLS and _backup files) in the version control settings. (.gitignore- Basically track only important concepts)

Version Control Best Practices: Established a clean repository structure by configuring Git to track only essential test metadata and scripts, ensuring the framework is ready for team collaboration and CI/CD integration.

