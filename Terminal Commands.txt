Terminal Commands


just have the correct directory n have the correct files ready to commit

git checkout <branchname>

cd .. -  go back directory 

dir -  show list of all directories

mkdir brand_new_folder   //mkdir <folder name> create new folder.

cd brand_new_folder         // change directory 

pwd - Shows current folder path            (in powershell )

ls - Shows WHAT is inside the folder           (in powershell)

developer of git - Linus 

was acquired by Microsoft in 2018 for usd 7.5 billion 

GitHub - host in a repository for us.


git config --global user.name "abhi"

Microsoft owns GitHub 

git status ( to make sure folder is not a repository)

git init (initialize .git file its hidden)    --- create a new GitHub repository.

git add <filename.txt> // single file is added.  aka gets added to the STAGING AREA.

to unstage <file.txt>,

git restore --staged <file.txt>
           //gap   //gap

glob pattern - .git is hidden 
 

here’s a clean, no-nonsense reset sequence to fix your repo in one go. This will get you perfectly synced with GitHub.

⚠️ This will delete ALL your local changes (uncommitted + messy rebase state).
If that’s fine, proceed.

Your repo is inside OneDrive, which causes all these issues.

👉 Best move:

C:\projects\testing01

Then:

git clone https://github.com/abhi-trumpf/testing01.git


Find the real Git root ,

Run this:

git rev-parse --show-toplevel

👉 This will show the actual folder Git is tracking



Problem - Your Git repo is initialized at the root of your C drive (C:/), not in your project folder.

Every Git repository has one .git directory.

git branch -m master main // rename your current branch from master to main
2. Get remote updates safely

PULL AND PUSH 
git pull origin main --rebase
3. Push correctly
git push -u origin main


git remote remove <name>: This deletes the connection to a specific remote repository entirely from your local configuration.
//with relation to repository

Rename local branch -m <old-name> <new-name>                  //all of this is with respect to branch
Delete local branch git branch -d <branch-name>

git remote rename <old> <new>        //change repo name (remote na le - with respect to repo)
git remote remove <name>     // deletion of repo.

git log - show history of repo
check for remote branch git remote -v

git remote add name <GitHub link>       //no
git remote add origin <GitHub link>

git remote -v shows the mapping between your local repository and the remote server.

git fetch / git fetch origin -  shows all the changes made to your directory by other people.


To switch from one branch to another. //   cs-bc
  
git checkout <branchname>     // switch to an existing branch   CHEKOUT IS imp

git checkout -b <new-branch-name>  // create a new one and switch to it


git switch <branchname>     // switch to an existing branch ex                  git switch master

git switch -c <new-branch-name>  //create a new one n switch immediately 


git branch      -  show current branch of repo

git branch -a   -  show all branches in repo 

git remote -v -  To remove the connection between your local repository and a remote repository (like GitHub)

git branch <new branch> - create a new branch 

git branch -m <branch name> - delete a branch

git merge <branchname> - the changes from GitHub will get updated to your local files ?   remote repo to local repo // IMPORTANT related to branch Git takes the changes you made in that side-path and pulls them into master.

<branchname> - side branch gets added to whatever current Mainbranch is.

Command,Direction,Relationship
git push,Local → Remote,Sends your branch's changes to GitHub.
git pull,Remote → Local,Grabs updates from GitHub and puts them in your local branch.
git merge,Local ↔ Local,Merges one branch (side-path) into another branch on your PC.

git push -u origin <name>  //local repo to remote repo

git checkout master
git status
git pull origin master  Exit Code 0: This means "Success" (like returning true).

                       Exit Code 1 (or higher): This means "Error/Conflict" (like returning false).

git checkout master - Switch my local view to the branch named master."

The rule is simple: Git merges "Whatever branch I am currently on" with "Whatever branch name I type in the command."

1. The "Current Branch" (The Destination)
Before you run a merge, you must be "checked out" on the branch where you want the code to land.

If you want to merge into master, you must first run: git checkout master

If you want to merge into main, you must first run: git checkout main

2. The "Target Branch" (The Source)
You can then merge from any other branch into that destination.

Examples of valid commands:

git merge main (Merging the main branch into your current branch).

git merge master (Merging the master branch into your current branch).

git merge testing01 (Merging your testing01 branch into your current branch).

git merge <branch name> 

. It only checks for clashes the moment you try to bring them together.
When you run git merge, you are merging two different timelines into one.

Think of it this way:

Branch A (Your destination): Where you are right now (usually master or main). This is your "official" record.

Branch B (Your source): The branch you are typing in the command. This is your "new work."

   A + B gives correct c or C with conflicts 

Correct C  - successful or complete 

C with conflicts - unsuccessful or incomplete

In the Git world, we don't usually call them "incomplete merge branches"; instead, we say you are in a "Merge State" or a "Conflict State."   //imp

Here is a quick way to keep those three concepts organized in your head:

The State (Pausing): Git has stopped the train. It won't move forward until you manually fix the tracks.

The List (Flagging): This is your To-Do List. Any file you see when you type git status is a file that must be cleaned up before the train can start moving again.

The Mess (Dirty Files): This is the physical evidence of the conflict. The <<<<<<< and >>>>>>> markers are Git's way of showing you exactly where the two branches disagree.

If you leave your repository in that "Merging" (or conflict) phase, nothing terrible happens to your computer, but it does prevent you from doing other Git work.

Here is exactly what "stuck" means in practice:  --------------------------- MERGE STATE 

1. You cannot perform other Git operations
If you try to do other things while in this state, Git will stop you. For example:

You cannot switch branches: If you try git checkout <other-branch>, Git will say, "I can't let you switch until you finish resolving the conflicts in this current merge."

You cannot pull updates: If you try git pull, Git will say, "You have unmerged files."

2. Your project won't build (if it’s a C# project)
As we discussed, those <<<<<<< and >>>>>>> markers are not valid C# code. If you try to run your project, the compiler will see those symbols and say, "Syntax error," and your code won't run.

TO GET OUT OF MERGE STATE ,

1.Finish it: Resolve the markers, git add, and git commit. (This creates the "Merge Commit" and officially finishes the merge).

2.Cancel it: Run git merge --abort. (This cleans up the mess and puts the train back to where it started).

"Conflict" isn't a type of branch
There is no such thing as a "conflicted branch" or a "non-conflicted branch." There is only a merge result and its called MERGE STATE .


SO checkout is basically a better version of switch 
checkout deals with files commits and branches

switches only deal with branches.

git add .
git commit -m "Merged <branchname> into master and resolved conflicts"
git push origin master


When you run git diff, it compares two things:                     // write code and practice 

The code currently in your files (what you see on your screen right now).

The code as it existed in your last commit (the "clean" version that Git has saved

git checkout master
git status
git pull origin master
git merge <branch-with-conflicts>
git add .
git commit -m "Merged <branchname> into master and resolved conflicts"
git push origin master
 uhh  

When inside vim black static screen,
Shift + ZZ

git log -> tell everything 

git log --oneline -> just the highlights.


branch which points to a commit 

git checkout <branchname> - switch current branch to a different one.

git checkout <commit hash> - view your past safe point without making changes to the current work. (while maintaining the remaining commits)

git restore <filename> - basically a ctrl + Z // only to the last checkpoint.

git reset <particular commit/particular hash>   // resets back to particular checkpoint.  Changes to current files remains unchanged and Reset the commit only (if u commited to wrong branch)

            Repositioning the HEAD pointer to a different commit hash
Reset Type,Moves Local Repo?        Keeps Staged Code?     Keeps File Content?
--soft            Yes                 Yes                      Yes
--mixed           Yes                 No                       Yes
--hard            Yes                 No                       No


4 step process  ---------
Working directory - unsaved drafts of your code.

git add of working directory gives staging area.

git commit of staging area - local repo (this is where current COMMIT lives)

git push of local repo - remote repo.


 
can lose shared history - git reset --hard	You force the branch to an older commit and "delete" everything after it. 

git revert <commit hash>- historical state of previous commit 

very similar to git reset.
 
git switch -c <name> = Create a new branch and Switch to it immediately. This turns a temporary "Detached HEAD" state into a permanent, named branch.

let’s start a brand new timeline right here and call it 'new-feature-name'." 

you are standing at an old point in history (11db4d9).

git switch -c my-fixed-code

git push -u origin my-fixed-code

The Main Road (master): This is still sitting further ahead, where you most recently worked on your testing01 repository.

The Side-path (my-fixed-code): When you run git switch -c my-fixed-code, you are creating this new road starting from that old spot.



Feature,git checkout <hash>,git revert <hash>
History,Moves you back to an old point (Time Travel).,Adds a new point (commit) to the end of the line.
Safety,"Puts you in ""Detached HEAD"" (temporary/ghost state).",Safe; you stay on your current active branch.
GitHub,Usually not pushed (used for local viewing only).,Pushed to GitHub like a normal update/fix.

"Reflog" (a secret history) of everywhere you’ve been.

Your Current Workflow SuccessYou successfully moved from a Detached HEAD $\rightarrow$ to a New Branch (my-fixed-code) $\rightarrow$ to a Clean Push to GitHub. That is exactly how a pro handles it.

how to get detached head ??? 

Find your history:
git log --oneline

Checkout the hash:
git checkout <hash> (e.g., git checkout 11db4d9)

detached head-> new branch -> clean push

on being whichever temp branch you want,
git Stash - when you cant warrant a full commit from working directory to staging area , also called as temporary shelves or stashes.  // like putting things inside a box

git switch main 
git stash pop                     // box is being opened.

git remote  - kinda like bookmark.

git remote -v git connections n url.

git remote add <name><url>

directory_name/ - ignore everything in directory.

*.sql - ignore everything ending with .sql

pull requests is similar to git merge.
in open pull request,

Action,Command/Location,What it actually does
Pull,git pull origin,Moves code: Cloud → Your Laptop.
Push,git push origin,Moves code: Your Laptop → Cloud.
Pull Request,GitHub Website,Asks the owner to merge your code into theirs.

1. The Command (git pull) = Taking  remote repo to local repo.

2. The Pull Request (PR) = Giving
A "Pull Request" is a proposal. You are going to the original owner and saying:

"I have made some cool changes. Would you like to pull them into your project?"

The YAML file is essentially the "Rulebook" or "Instruction Manual" for GitHub's robots.
Only really useful for big projects with a lot of activity that needs to be done automatically - GitHub Actions

 create pull request ---  basically I have made some changes and I want to add them to your branch of repo.


git pull origin <branchname>: This is the most direct way. It grabs the changes from the GitHub "Warehouse" and merges them directly into your local "Workbench".

git fetch: This tells the computer: "Go look at GitHub and see if there are any new changes, but don't touch my files yet."

git merge: This tells the computer: "Take the changes I just 'fetched' and combine them into my current work."

git pull: This is a shortcut. It does Fetch + Merge at the same time.

git pull origin <branchname>: This is the most direct way. It grabs the changes from the GitHub "Warehouse" and merges them directly into your local "Workbench".
"Workbench" is your Local Branch.

git log: show history of all the commits ah // history book of all your savepoints.


ren "oldname.txt" "newname.rtf" - rename file_name and format.

Override your version over the cloud/remote repo
git checkout --ours "myrtffile_c#notes.rtf"
git add "myrtffile_c#notes.rtf"
git commit -m "Resolved conflict by keeping local version"
git push

1. --ours (Your Local Version)
This refers to the version of the file sitting on your laptop right now.

It is the code/notes you were just working on before you tried to pull.

In your case, this is the version that contains your "Abhishek" name and whatever you recently typed.

2. --theirs (The Remote/GitHub Version)
This refers to the version coming from the server (GitHub).

Since you are doing a git pull, "they" are the people (or the other version of you) who pushed changes to the cloud earlier.

git checkout --theirs "myrtffile_c#notes.rtf"
git add "myrtffile_c#notes.rtf"
git commit -m "Resolved conflict by keeping remote version"
git push

override remote repo over local files

when conflict arises btw local and remote repo ^




