  *return new int[] { };  - first square bracket then curly braces.
  *let wont come just int rem = 0 , int c = x.    1%10 = 1.
  *foreach (int num in testNumbers) {
            bool result = obj.IsPalindrome(num);
            Console.WriteLine($"Is {num} a palindrome? {result}");
  *for(int i=s.Length-1;i>0;i++)// Length -- L caps  
  *int length = nums.Length;
  * if(nums[i]>=target)

   *  int [] num3= new int[m + n];

   *  if (nums.Length == 0) return null;   // ✅ base case

   * var tree = new TreeNode(nums[mid]);  // num   // creation of manipulation of array VAR IS Important 
        tree.left =  SortedArrayToBST(nums[..mid]);  //2 dots  t CAPS
        tree.right = SortedArrayToBST(nums[(mid +1)..]);

    *int n = int.Parse(Console.ReadLine());   //;  - take input // ipcr  P caps 
   *  Console.Write("");  //

    *       for(int i=0;i<n;i++)
            {
            for(int j=0;j<i;j++)   //only after selling 
            {
                int res = prices[i]-prices[j];
            }
            }

 *     int[] n2 = new int[] {2,2,1};

 *    using System.Collections.Generic;   // we need to include for using .Add()
      List<int> list = new List<int>();
      list.Add(3);    // ✅ Works

*      if(num == candidate)  // look at spacing 
*    

*      Index:   0   1   2   3   4   5
Value:   1   2   3   1   2   3
Compare:
0-3 -> 3 > 2 ❌
1-4 -> 3 > 2 ❌
2-5 -> 3 > 2 ❌
No pair satisfies ≤ k → false

*using System;
using System.Collections.Generic;

List<int> numbers = new List<int>() { 1, 2, 3, 4 };

nums is an array (int[]), not a List<int>. You cannot do nums.Remove(i) or nums.Add() on an array. Arrays have fixed size.
*numbers.Add(5);
*numbers.Insert(2, 10);
*numbers.Remove(10);

public class Solution {
    public void MoveZeroes(int[] nums) {
        int n = nums.Length;
        List<int> numbers = new List<int>(nums);
        int size = numbers.Count();
        List<int> numbers2 = new List<int>();
        int size2 = numbers2.Count();
        int [] new3 = new int[size+size2];
        for(int i=0;i<n;i++){
            if(nums[i]==0)
            {
                numbers.Remove(i);
                numbers2.Add(i);
            }
        for(int j=0;j<=n;j++)
        {
            new3[j]= nums[j];
        }
        for(int k=0;k<n;k++)
        {
            new3[size2+1] = numbers[k];     //DONT DO 
        }
        }
    }
}

// WHAT TO DO?????

public class Solution {
    public void MoveZeroes(int[] nums) {
        int insertPos = 0; // position where next non-zero will go

        // Step 1: Move all non-zero elements to the front
        for (int i = 0; i < nums.Length; i++) {
            if (nums[i] != 0) {
                nums[insertPos] = nums[i];
                insertPos++;
            }
        }

        // Step 2: Fill the rest with zeros
        for (int i = insertPos; i < nums.Length; i++) {
            nums[i] = 0;
        }
    }
}




The null comes from the constructor, NOT from prefix.


public class Solution {
    public int[] Intersection(int[] nums1, int[] nums2) {
        HashSet<int> s1 = new HashSet<int>(nums1);             // S caps //after new use a backspace 
        HashSet<int> s2 = new HashSet<int>(nums2);
        s1.IntersectWith(s2);   //IW             s1.Add(x); (within a for loop)
        return s1.ToArray();        //TA     

    }
}



   for (int i = 0; i < n; i++) {                                                       // remove element from array manually 
            if (nums[i] != remove) {
                newArray[j] = nums[i];
                j++;
            }
        }

Console.WriteLine($"The first distinct maximum is {max}");

'''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''
using System;

public class Solution {
    public int ThirdMax(int[] nums) {
        int n = nums.Length;

        // -------- Step 1: Find first maximum --------
        int max1 = nums[0];
        for (int i = 1; i < n; i++) {
            if (nums[i] > max1) {
                max1 = nums[i];
            }
        }
        Console.WriteLine($"The first distinct maximum is {max1}");

        // -------- Step 2: Remove first max --------
        int[] newarr1 = new int[n - 1];
        int index = 0;
        for (int i = 0; i < n; i++) {
            if (nums[i] != max1) {
                newarr1[index] = nums[i];
                index++;
            }
        }

        // -------- Step 3: Find second maximum --------
        int max2 = newarr1[0];
        for (int i = 1; i < newarr1.Length; i++) {
            if (newarr1[i] > max2) {
                max2 = newarr1[i];
            }
        }
        Console.WriteLine($"The second distinct maximum is {max2}");

        // -------- Step 4: Remove second max --------
        int[] newarr2 = new int[newarr1.Length - 1];
        index = 0;
        for (int i = 0; i < newarr1.Length; i++) {
            if (newarr1[i] != max2) {
                newarr2[index] = newarr1[i];
                index++;
            }
        }

        // -------- Step 5: Find third maximum --------
        int max3 = newarr2[0];
        for (int i = 1; i < newarr2.Length; i++) {
            if (newarr2[i] > max3) {
                max3 = newarr2[i];
            }
        }
        Console.WriteLine($"The third distinct maximum is {max3}");

        // -------- Step 6: Return third maximum --------
        return max3;
    }
}


---------------------------------------------------------------------------------------------

public class Solution {
    public int ThirdMax(int[] nums) {
        int n = nums.Length;
        int max = nums[0];
        int i;
        for(i=1;i<n;i++){                          //finding max 
            if(nums[i]>max){
                max = nums[i];
            }
            Console.WriteLine($"The first distinct maximum is {max}");   ///finding max of array
        }
        //remove max from array
        int[] newarr = new int[n];
        int index =0;
        for(int j=0;j<n;j++){
            if(nums[j]!=max){
                newarr[index++]=nums[j];
            }
        }
        //// finding 2 nd max 
        int max2 = newarr[0];
        for(int k=1;k<index;k++)
        {
            if (newarr[k]>max2) {
                max2=newarr[k];
            }
        }
        int[] newarr2 = new int[n];
        int index2=0;
        ///// remove 2nd max
        for(int l=0;l<index;l++)
        {
             if(newarr[l] !=max2){       // l coMES HEREEEE
                newarr2[index2++] = newarr[l];     //index2 comes here and l here 2
            }
        }
        if (index2 == 0) {
            // No third max exists, return first max
            return max;
            }

        /// find 3 rd max
        int max3 = newarr2[0];
        for (int m=1;m<index2;m++){
            if(newarr2[m]>max3){
                max3 =newarr2[m];
            }
        }
        return max3;
    }
}
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
        string s2 = "my name is Abhishek";
        string[] words= s2.Split(' ');   // S caps                                                                                    //need to split by spaces 
        foreach (string word in words){
            if(word == "name")
            {
                Console.WriteLine("hhhaaah");
            }
            Console.WriteLine(word);  //c caps
        }
    }
}


/////////////////////////////////////////////


string[] s2 = s.Split(' ', StringSplitOptions.RemoveEmptyEntries);

/////////////////////////////////////////////////

using System;
using System.Text.RegularExpressions;

public class Solution {
    public bool IsPalindrome(string s) {
        // Step 1: Remove non-alphanumeric characters and convert to lowercase
        string cleaned = Regex.Replace(s, @"[^a-zA-Z0-9]", "").ToLower();

        // Step 2: Reverse the cleaned string
        char[] array = cleaned.ToCharArray();
        Array.Reverse(array);
        string reversed = new string(array);

        // Step 3: Compare
        bool isPalindrome = cleaned == reversed;
        Console.WriteLine($"\"{s}\" is{(isPalindrome ? "" : " NOT")} a palindrome");
        return isPalindrome;
    }
}


////////////////////////////////////////////
public class Solution {
    public int TitleToNumber(string columnTitle) {
        int total =0;
        foreach(var c in columnTitle){
            total = total*26 +(c-'A'+1);   // imp total = total *26 
        }
        return total;
    }
}


////////////////////////////////////////////
MAKE LIST FROM STRING 


List<char> l1 = t.ToList();       //t.ToList()  t is string t.ToList();

/////////////////////////////////
using System;
using System.Collections.Generic;

///////////////////////////
(!l2.Contains(c))

TryAdd	“Add if not present”
ContainsKey	“Check if already exists”
dict[key]	“Get value”

/////////////////////
👉 Two letters mapping to SAME word
👉 Violates rule: unique mapping

if(m2[day] = a)  MEANS-----
m2[a:"day"]
/////////////

public class Solution {
    public bool WordPattern(string pattern, string s) {
        string[]words = s.Split(' ');
        if(pattern.Length!=words.Length){
            return false;
        }
        Dictionary<char,string> d1= new Dictionary<char,string>();
        Dictionary<string,char> d2 = new Dictionary<string,char>();

        for(int i=0;i<pattern.Length;i++)
        {
            char p = pattern[i];    // chars of pattern
            string s2 = words[i];   // string of s 
            if(d1.ContainsKey(p))
            {
                if(d1[p] != s2)   //d1[p] 👉 This gives you the VALUE, not the key, not the pair.
                    return false;
            }
            else
            {
                d1[p]=s2;
            }
            if(d2.ContainsKey(s2))
            {
                if(d2[s2] != p)
                return false;    
            }
            else{
                d2[s2] = p;
            }
        }
        return true;
    }
}


Function Overloading - same function name diff parameters 

void print(int a);

void print(float b);


Function Overriding - change the print statement 

class 1{
print(){
console.log("what is the meaning of life");     // javascript 
}
}

class 2 extends 1{                                // different class name but same function name
print(){   
console.log("the whole point of life is to chill");
}
}


in c#, we are going to do --------- public void print(){}


string str = "Hello world C sharp";
string[] words = str.Split(' ');


     s2[i]=s[n-1-i];   // i starts from 0.  s [0] = s[5-1-0] = s[4]



using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
        string s = "abcdefg";

        if (s.Contains('a') || s.Contains('e') || s.Contains('i') || s.Contains('o') || s.Contains('u'))   // C caps brackets and ' '
        {
            Console.WriteLine("Vowels found");             //C,W,L Caps 
        }
    }
}

return haystack.IndexOf(needle);         big part.IndexOf(needle)

if (haystack.Substring(i, needle.Length) == needle)


if ("AEIOUaeiou".Contains(c))        // C caps 

foreach (char c in s2)    // foreach and then gap 

result[i] = s2[j] → replaces the current vowel in the original string with the vowel from the end.

Character	Is vowel?	Action	Resulting str
h	No	Append h	h
e	Yes	Pop o	h o
l	No	Append l	h o l
l	No	Append l	h o l l
o	Yes	Pop e	h o l l e


public class Solution {
    public string ReverseVowels(string s) {
        var str = new StringBuilder();
        var l = new Stack<char>();

        foreach(var i in s){
            if("aeiouAEIOU".Contains(i))
                l.Push(i);
        }

        foreach(var i in s){
            if("aeiouAEIOU".Contains(i))
                str.Append(l.Pop());
            else
                str.Append(i);
        }
        
        return str.ToString();
    }
}

////////////////////////

public class Solution {
    public string ReverseVowels(string s) {
        string vowels = "AEIOUaeiou";
        string s2 = "";  // collect vowels

        foreach (char c in s) {
            if (vowels.Contains(c)) {
                s2 += c;
            }
        }

        char[] result = s.ToCharArray();
        int j = s2.Length - 1;  // index for reversed vowels

        for (int i = 0; i < result.Length; i++) {
            if (vowels.Contains(result[i])) {
                result[i] = s2[j];  // replace vowel with reversed vowel
                j--;
            }
        }

        return new string(result);
    }
}

///////

bool() is not a constructor so cant do bool(magazine.IndexOf(ransomNote));)

if present na 1 or else -1 plus it will only return integer not a boolean value.


if (freq.ContainsKey(c)) --- .Contains nope .ContainsKey correct 


Character	Count
l	2        // 1 - consonent 
o	2
v	1
e	4
t	1
c	1
d	1



using System;
using System.Linq;

 string s = "abcdef";
        string shuffled = ShuffleString(s);

Console.WriteLine($"Values before swapping are {n1} and {n2}\n");

n3.Length → ❌ int has no Length ---> only possible if n3 is a string 

string s3 = "ahahah";
int x = s3.Length; // property not a method   cant do s3.Length() thats wrongg.

string s3 = Console.ReadLine();   // no need parse for this one

Write() → same line
WriteLine() → next line


using System;
public class yes{
    public static void Main(){
        int n =4;
        for(int i=0;i<n;i++){
            for(int j=0 ; j<=n-i; j++){   //<=
                Console.Write(" ");  //left space 
                
            }
            for (int k = 0; k <= i; k++)  //<=
            {
                Console.Write("#");   //left hashes 
                
            }
            Console.Write("  ");
            for (int l=0 ; l<=i; l++)
            {
                Console.Write("#");
            }
            Console.WriteLine();    //only here WriteLine comes
        }
    }
}
  && - and ,|| - or 

HelloWorld c1 = new HelloWorld();               //classname obj = new classname();

public class new2{ 
                                   ....
}// no() for class  

    public static void Main(string[]  args)  // M caps ad after string we use []


/// new stuff
Even though you used $, you didn’t put max inside {}.

👉 $"" only works when variables are inside {}
👉 Here "lcm is" has no placeholder → max is ignored

✅ Correct version
Console.WriteLine($"lcm is {max}");


C# array → fixed size → no push/pop
C# List<T> → dynamic → use .Add() / .RemoveAt() like push/pop concept

No — in C#, .Length is not just for strings. It works for arrays of any type: int[], double[], char[], etc. 
int x = new Array[];


// 153 → 1³ + 5³ + 3³ = 153 ✅
9474 → 9⁴ + 4⁴ + 7⁴ + 4⁴ = 9474 ✅  // Armstrong Number 


//Math.Pow(2,4) --- 2* 4  = returns a doubly value 

Leap year logic 

A leap year is:

Divisible by 400
OR
Divisible by 4 AND NOT divisible by 100


for taking strings as input 
   Console.WriteLine("enter the string");
   a = Console.ReadLine();

(char c of s8)         //if u want chars in string


string[] words = s.Split(' ');   // if u want words instead of characters 


👉 C# ALWAYS needs:

project OR
script runner  // imp


int.TryParse(choiceInput, out choice)
tries to convert input into a number

Examples:

Input	Result
"1"	✔ success → choice = 1
"25"	✔ success → choice = 25
"abc"	❌ fail
""	❌ fail


2. ! (NOT operator)

This flips the result:

TryParse result	With !
true (valid input)	false
false (invalid input)	true


Why out choice and not just int choice?

Because TryParse is designed to do TWO things at once:

Tell you if conversion worked (true/false)
Give you the converted number



int.Parse("abc") ❌ crash
int.Parse(null) ❌ crash

Good question — this is where things finally “click” in C#.

🔥 What “No more Parse used anywhere” means

It means:

👉 You are NOT using this unsafe method anymore anywhere in your code:

int.Parse(input)
🧠 Why Parse is a problem
int.Parse("abc") ❌ crash
int.Parse(null) ❌ crash

👉 It assumes:

“Input is always a valid number”

But real users don’t behave nicely 😄

🔥 What you replaced it with
int.TryParse(input, out n)
🧠 Why TryParse is better
Feature	Parse	TryParse
crashes on invalid input	❌ yes	❌ no
handles null	❌ no	✔ yes
safe	❌ no	✔ yes
returns success/fail	❌ no	✔ yes
💡 Simple meaning of that line
int.TryParse(input, out n)

👉 means:

“Try converting input into a number.
If it works, store it in n.
If it fails, don’t crash.”

🔥 So when I said:

“No more Parse used anywhere”

I meant:

👉 You have completely removed ALL unsafe conversions like:

int.Parse(...)

and replaced them with:

int.TryParse(...)
🧠 One-line summary

👉 “No more Parse used anywhere” =

You are now using only safe input conversion in your whole program.

⚠️ Key rule
Language	File type
C#	.cs
C	.c

TryParse actually returns two things:

true/false → whether conversion succeeded
the parsed integer → via out


int[] arr = new int[11];

for (int i = 0; i < 11; i++)
{
    Console.Write($"Enter value for arr[{i}]: ");
    int.TryParse(Console.ReadLine(), out arr[i]);
}


int[11] arr6;       // this is wrong
int[] arr6 = new int[11];    // this is correct.

long[] new7 = new long[11];     // big  values wont become 0.

/tmp/G27RI2YysH/Main.cs(743,1): error CS1022: Type or namespace definition, or end-of-file expected - bracket mismatch 

double avg = (double)sum / n;     / 5/2 = 2.5 n not 2 

Using malloc (runtime allocation)
int *ptr = (int *)malloc(10 * sizeof(int));
You’re asking the system: “Give me memory for 10 integers”
Memory is allocated during execution
Size can be decided dynamically

free(p) -  if its not used,
then not freeing memory , its going to causes a memory leak.


.sln	Solution (container for projects)
.vcxproj	C/C++ project
.c / .cpp	Actual source code


🔥 VALID examples

These are valid numbers:

Input	Why
"10"	pure integer
"0"	valid integer
"-5"	negative integer is still valid
"100000"	valid
" 25 "	spaces are ignored, still valid
❌ INVALID examples

These are NOT valid numbers:

Input	Why
"abcd"	not numeric
"12.5"	decimal (not int)
"10a"	mixed characters
"" (empty)	nothing entered
"@#"	symbols

Valid and invalid numbers 

Array.Reverse(a);
Array.Sort(a);

Finding index
int index = Array.IndexOf(a, 10);
6. Finding last index
int index = Array.LastIndexOf(a, 10);
7. Checking existence
bool exists = a.Contains(10); // needs System.Linq
8. Getting max / min
int max = a.Max();
int min = a.Min();
9. Sum of elements
int sum = a.Sum();

a.Distinct()  // remove dupicate from array


🧠 Why HashSet is powerful

✔ automatically removes duplicates
✔ very fast (O(n))
✔ commonly used in interviews + production

ONLY HAVE UNIQUE VALUES 

using System;
using System.Collections.Generic;

public class Program
{
    public static void Main()
    {
        int[] a = { 1, 2, 2, 3, 4, 4, 5 };

        HashSet<int> set = new HashSet<int>();

        foreach (int x in a)
        {
            set.Add(x); // duplicates automatically ignored
        }

        Console.WriteLine("Unique elements:");

        foreach (int x in set)
        {
            Console.WriteLine(x);
        }
    }
}

✅ What is using System.Collections.Generic;?

It allows you to use generic collection classes like:

List<T>
Dictionary<TKey, TValue>
HashSet<T>
Queue<T>
Stack<T>

using System.Linq  why we need this one????


You need it only when you use LINQ methods like:

✔ Distinct()
✔ Where()
✔ Select()
✔ Max()

if (int.TryParse(input, out num) && num > 0) - 1st part - converts string to integer and returns true only if the conversion is successfull.
                                         2nd part - checks if number is greater than 0.


string sentence = "I love programming in Csharp";

        string[] words = sentence.Split(' ');

        foreach (string word in words)
        {
            Console.WriteLine(word);
        }

Python - higher order programming language


int *tmp = malloc(4 * sizeof(int));

This is allocating memory for an array of 4 integers, and tmp is a pointer to the first element of that array.

Break it down:
malloc(4 * sizeof(int))
Allocates enough memory to store 4 integers
Returns a void* pointer to the beginning of that block
int *tmp
Declares a pointer to int
So tmp will point to the first integer in that allocated block
So what is it exactly?

👉 Both, in a way:

tmp → is a pointer
The memory it points to → behaves like an array of 4 integers
You can use it like an array:
tmp[0] = 10;
tmp[1] = 20;
tmp[2] = 30;
tmp[3] = 40;

int *tmp = malloc(4 * sizeof(int));

tmp → points to first integer
✔️ tmp[0] → first integer
✔️ *tmp → also first integer

👉 Meaning:

tmp[0] == *tmp
Example:
tmp[0] = 10;

printf("%d\n", *tmp);    // 10
printf("%d\n", tmp[0]);  // 10
Simple memory view:
tmp ──► [10][20][30][40]
         ↑
        *tmp

printf("%d\n", *(tmp + 1));  // 20

if you use malloc you should always free up spacee.

stdio.h - printf,scanf

stdlib.h - list values
malloc()
calloc()
realloc()   - by  default  will  return 0.  will allocate 4 bytes/n no of bytes then it will 
what memeory has been allocated to u in the past.
free()


free(list)
tmp[3] = 4;
free(list) 

you are freeing up memory in different list locations (temp[0] to temp[2] then we are moving to temp[3])

Meta-data - helps you maintain data you care about.

Linked List is called Vector in other languages.

(*n) - dereferance n and then go inside it.
*n.number // is called dereferancing it.

* n is the same as n->next = NULL;
n->number = 5; // or u can use get_int("enter number");

node * n = malloc(sizeof(node)); 

|__|------>|_|
 n         |_| node 


list = the starting node (head) of the linked list
node *ptr = list ;


"The ptr != NULL",
                                        check acts as a guardrail, preventing the program from crashing by
 ensuring you don't try to access data once you've run out of nodes."


hiiii


| Header     | Purpose                        |
| ---------- | ------------------------------ |
| `stdio.h`  | input/output                   |
| `stdlib.h` | memory, utilities, conversions |

change github branch from master to main
git branch -m master main
git push -u origin main

pull the remote changes into your local files, resolve any conflicts, and then push.

git pull origin main --rebase

git push origin main

In Git, origin is just a nickname (an alias) for the URL of your GitHub repository. Even if your folder or project is named testing01, the connection to your GitHub repository is almost certainly named origin.

You can verify this by running:

Bash
git remote -v
This command will show you the name (usually origin) and the web address (https://github.com/abhi-trumpf/testing01.git).

Why we use the name origin

// If in middle of list
if (m->number < ptr->next->number)
{
    m->next = ptr->next;
    ptr->next = n;
    break;
}

unload(list) -  free memory

leaf node - no children.

Bst- Binary search tree - ez to add new nodes cuz no need to copy data 1 by 1.Downsise - less speed and dynamism.More Memory 

Array of linked List - Hash Table 

adding new content for ref\

string[] myData = { "apple", "apple", "banana" }; is the standard way to declare and initialize a fixed-size array in C#.

string[] result = myData.Distinct();   returns something called an IEnumerable<T>.

Distinct() not as a "physical box," but as a smart magnifying glass or a filter that you place over your existing data.

pointer (the IEnumerable) that knows how to show you only the unique ones when you start looking.


using System;

class ResourceHandler
{
    // Constructor
    public ResourceHandler()
    {
        Console.WriteLine("Object created: Resource initialized.");
    }

    // Destructor (Finalizer)
    ~ResourceHandler()
    {
        Console.WriteLine("Object destroyed: Resource cleaned up.");
    }
}


Default Constructor: When constructors do not have parameters, then it is called the default constructor. These types of constructors have all its instance initialized with the same value.

Parameterized Constructor: When any constructor has at least one parameter, it is called the parameterized constructor.

Copy Constructor: When the constructor is used to create an object just by copying all of its variables from another object, such constructors are called copy constructor. They are used for initializing a new instance from an existing one.

ex

public class MachinePart
{
    public string Name { get; set; }        // { get; set; }  This is the "magic" part that makes them Auto-Properties:

get: Allows other parts of your code to read the value. Example: string myName = myPart.Name; set: Allows other parts of your code to change the value. Example: myPart.Weight = 2.5;

    // Standard constructor
    public MachinePart(string name, double weight)
    {
        Name = name;
        Weight = weight;
    }

    // The Copy Constructor
    public MachinePart(MachinePart other)
    {
        // Copying values from the 'other' object to the new instance
        this.Name = other.Name;
        this.Weight = other.Weight;
    }
}

// Usage:
MachinePart originalPart = new MachinePart("Laser Nozzle", 0.5);

// Creating a new instance using the Copy Constructor
MachinePart clonedPart = new MachinePart(originalPart);

// Now clonedPart has the same values as originalPart

Private Constructor: When a constructor is produced with a private access modifier, it is called Private Constructor. It does not make it possible for other classes to inherit any data from this class.

Static Constructor: When a constructor needs to be invoked only once, and when that constructor needs to be invoked at creating the first reference, then those constructors are made static and are called static constructors.


using System;

public class Person
{
    public string Name;

    // Constructor
    public Person(string name)
    {
        Name = name;
        Console.WriteLine("Constructor executed for: " + name);
    }
}

class Program
{
    static void Main()
    {
        // Constructor is called here when creating the object
        Person p1 = new Person("Alice");
        Person p2 = new Person("Bob");
    }
}





Destructor is another method that uses the class-name but is preceded with a ~ (tilde) operator/symbol. Destructors are used to de-initialize objects, and the memories occupied when constructors are created. You can consider them as the opposite of constructors.

public class EmpGkr
{
    int start;
    //constructor
    public EmpGkr()
    {
        val = 10;
    }

    //destructor
    ~ EmpGkr()
    {
        val = 0;
    }
}


C# is single inheritance        
. C# (Object-Oriented Language)  oops concept 
C# is built from the ground up to support the Object-Oriented Programming (OOP) paradigm. It provides specific keywords and structures—like class, interface, inheritance, and encapsulation—that automatically manage objects for you.
Garbage Collected (Automatic)
         

C is multiple inheritance concept
C (Procedural / Structured Language)
C does not have class or object keywords.
Manually Managed

:wq

<<<<<<<
=======
>>>>>>> 

node *list = null ,pointer of node is null 

pointer variable name of node is list

HEAD
      (Pointer)
        |
        V
+-------------+      +-------------+      +-------------+
|    Node 1   |      |    Node 2   |      |    Node 3   |
|-------------|      |-------------|      |-------------|
| Data: 10    |      | Data: 20    |      | Data: 30    |
| Link: [*]---+----->| Link: [*]---+----->| Link: NULL |  <-- End of List
+-------------+      +-------------+      +-------------+


x = 10;	The integer 10.
sizeof(int)	The size of the Integer blueprint (usually 4).
sizeof(node)	The size of the Node blueprint (usually 12 or 16).

node *list = NULL;
node *ptr = list ; set node called pointer to BEGINNING of list/beginning of struct - same thing (imp)

A linked list is simply a collection of struct node instances.

node *temp = ptr  // ptr value store to pointer temp variable of node .


Hash table is basically an array.
Each array is a linked list.

By combining an array of linked lists you get the speed of an array with the flexibility of a linked list. 

soo array act as the SPINE and linked list are the branches of the spine -  Hash TABLE 

char *word: This is a pointer that holds the address of the start of the string. Start of the string orthe start address of the first char(both are the same thing).
points to start of a string

The function isalpha() is designed to check a single character (char), not an entire string or a pointer.

There is no string keyword in the C langauge.

 bucket number after combining 2 letters should be unique

Word	Letter 1 (c1)	Letter 2 (c2)   Simple Addition (c 1+ c2)  The Formula (c1*100) + c2
AC	      0 	    2	              2	                      2
BB	      1	            1	         2 (Collision!)	           101 (Unique!)
CA	      2	            0	         2 (Collision!)	           200 (Unique!)

public class Solution {

public int\[] Intersection(int\[] nums1, int\[] nums2) {
HashSet<int> s1 = new HashSet<int>(nums1);             // S caps //after new use a backspace 
 HashSet<int> s2 = new HashSet<int>(nums2);
s1.IntersectWith(s2);   //IW             s1.Add(x); (within a for loop)

    return s1.ToArray();        //TA     

array and linked list similarities



While arrays and linked lists are structurally very different, they share a common purpose in computer science. Since you’ve been exploring software development in C# and C lately—specifically through your testing01 project—understanding these similarities will help you choose the right tool for the job.



1\. They are both "Linear" Data Structures

The most fundamental similarity is that both are linear collections. This means they store elements in a specific order (a sequence).

In an Array, the order is determined by the physical memory slots (0, 1, 2, ...).

In a Linked List, the order is determined by pointers (node A points to node B).

2\. They Store the Same Data Type

Both structures are typically homogeneous.

If you declare an int array, it only holds integers.

If you define a struct node that contains a char \*word, every node in that linked list will store that same type of data.

3\. They Support the Same Basic Operations

Regardless of which one you use, you can perform the same "CRUD" operations on them:

Insertion: Adding a new element.

Deletion: Removing an element.

Traversal: Visiting every element from start to finish (like using a for loop).

Search: Looking for a specific value.



4\. They Both Use Memory

While their memory allocation styles differ (arrays use one big block; linked lists use scattered nodes), they both ultimately occupy space in your computer's RAM.

In C#, the Garbage Collector handles this memory for both structures.

In C, you have to be more mindful of how much memory each is consuming, especially as your project grows.
Summary Table

Feature	Array	Linked List

Logic	Linear Sequence	Linear Sequence

Access	Sequential or Random	Sequential

Data Types	Uniform (Homogeneous)	Uniform (Homogeneous)

Goal	Storing a list of items	Storing a list of items

Think of them like two different ways to store a grocery list: an array is a single piece of paper with numbered lines, while a linked list is a series of post-it notes where each one tells you where the next one is hidden. They both hold the same list of groceries!
how are they structurally different show in diagram
The main structural difference lies in how they occupy your computer's memory: Arrays are a solid block, while Linked Lists are scattered pieces connected by "addresses."
The Structural Diagram

Plaintext

MEMORY LAYOUT: ARRAY (The "Spine")

     +-------+-------+-------+-------+

Address| 0x100 | 0x104 | 0x108 | 0x112 |  <-- Contiguous (Next to each other)

    +-------+-------+-------+-------+

Data   |  'A'  |  'B'  |  'C'  |  'D'  |  <-- Same size, fixed slots

     +-------+-------+-------+-------+

Index  |   0   |   1   |   2   |   3   |

      MEMORY LAYOUT: LINKED LIST (The "Branches")
    +-------+----+      +-------+----+      +-------+------+

Node 1 |  'A'  |Ptr\*+----->|  'B'  |Ptr\*+----->|  'C'  | NULL |

  +-------+----+      +-------+----+      +-------+------+

Address|   0x205           |   0x880           |   0x412

Key Structural Differences

Memory Arrangement:
Array: Stored in **contiguous memory locations**. You can calculate the **exact location of any element instantly.*

Linked List: Stored in scattered memory locations. **Each node must store a "pointer" (the address) to find the next neighbor.**

Size Flexibility:

Array: Has a **fixed size**. To add more items than the array can hold, you usually have to create a brand new, larger array and copy everything over

Linked List: **Dynamic**. You can add a new "branch" or node at any time just by using malloc in C or creating a new object in C#.

Access Method:

Array: Supports Random Access. You can jump straight to index\[99] in one step.
Linked List: Only supports **Sequential** Access. To find the 100th item, you must start at the head and follow the pointers 99 times.

Array A: [1, 2, 3]
Array B: [4, 5, 6]

A.Concat(B) creates a virtual sequence: 1 → 2 → 3 → 4 → 5 → 6.
int[] merged = array1.Concat(array2).ToArray();
//Concat is "lazy"—it doesn't actually create a new list until you force it to. ToArray() is the force. It calculates the total size needed, allocates a new block of memory, and copies the numbers into it.

Actually, it's the other way around! While strings do have a .Length property, .Length is the standard way to get the size of any Array in C#.
int [] x = [1,2,3,4,5];
for(int i=0;i<x.Length;i++) //.Length does not have () 
{
   ...code
}

.Length   - property
.Length() - method

[1, 2, 3, 4, 5] is called a Collection Expression.
If you assign it to an int[], C# treats it exactly as an array.
C#
int[] myArray = [1, 2, 3, 4, 5];      //becomes an array 
Arrays should be assosiated with [].

// 
int n = 0, next;
while (n < 1000 && int.TryParse(Console.ReadLine(), out next)) arr2[n++] = next;

Expression,Index used for arr2,Final value of n,Effect on arr2[0]
arr2[n++] (Post),0,1,Gets the value (Current index used)
arr2[++n] (Pre),1,1,Skipped (Index updated first)


for (int i = 0; i < n; i++) 
{
    if (arr2[i] == 0) 
    {
        continue; // <--- Jumps straight to i++.
    }
}


for (int i = 0; i < n; i++) 
{
    if (arr2[i] == 0) 
    {
        break; // <--- Completely jumps out of for loop.
    }
}

int[] arr3; only creates a pointer (a name).
int[] arr3 = new int[1000]; // this is only correct.

/tmp/y0gUPU4rLG/Main.cs(25,50): error CS0103: The name 'i' does not exist in the current context - means i is outside for loop.

numbers = [1,2,3,4444,1];
Array.Sort(numbers);                 //S caps  dont use 
Array.Sort(arr2,i,0,n)


full code -> if (Array.IndexOf(arr2, i, 0, n) == -1)
============================================================
C# Array.IndexOf Syntax Breakdown: (arr2, i, 0, n)
============================================================

1. arr2  -> The "WHERE"
            The array variable name we are searching through.

2. i     -> The "WHAT"
            The specific value we want to find (our current loop number).

3. 0     -> The "START"
            The index where the search begins (the very first slot).

4. n     -> The "LIMIT" (Count)
            How many slots to check. This ensures we ONLY search 
            the numbers you entered and ignore the empty 0s at 
            the end of the 1000-slot array.

LOGIC:
If IndexOf returns -1, it means the number was NOT found.
============================================================

Array.IndexOf(...) — The Searcher
This is a built-in "Find" tool. Its only job is to look through the array and tell you where a value is.

If found: It returns the Index Number (0, 1, 2, etc.).

If NOT found: It returns -1.

Collection	Command	What it does	         Result/Return
HashSet	.Add(x)	Adds only if x is new.	         Returns true/false
List	.Add(x)	Adds x to the very end.	         Returns nothing
Dictionary	.Add(k, v)	Links a key to a value.	 Crashes if key exists



bool isNewNumber = seenNumbers.Add(currentNum);

// Try to add the number and save the result (True or False) if num doesnt exist it adds and returns true else it doesnt add and returns false 

While .c and .cpp files contain the actual instructions (the implementation), the .h file tells the compiler what functions, variables, and classes are available to use (the declaration). (.h - remember minion)

derefernce pointers on left side and access field on the right side.

Extension,Language,Purpose
.c,C,Source code (Implementation)
.cpp,C++,Source code (Implementation)                                          // instructions inrukum 
.h,C / C++,Header file (Declarations/Blueprints)
.hpp,C++,Header file (Specific to C++ standards)               // what are the components functions,varibles,classes and objects?


struct Player {
    int health;
};

Player hero;             // Instantiating an object for the class.
Player* ptr = &hero;     //Creating a  pointer holding the address of the object.

// Using dereference (left) and access (right)                                            
(*ptr).health = 100;                                                                                                     // same as the below code.

// Using the arrow shortcut (most common)
ptr->health = 100;

pointer dereferencing and assigning a variable to store value is an example of,

 ptr->health = 100                   (->)


Language,Do Pointers Exist?,                                                                  Can YOU touch them?
C,Yes,                                                                                                                        Yes.You have full control (and full responsibility).
C#,Yes,                                               Mostly No,The ""Garbage Collector"" manages them for you."
Python,Yes,                                                                                                            No. They are completely invisible.
JS,Yes,                                                                                                                      No. They are completely invisible.

The "Hospital" Analogy
Touching (C): You are the surgeon. You are physically touching the organs (memory). If you slip, it’s a disaster, but you can do things no one else can.

Not Touching (C# / Python): You are the general practitioner. You interact with the patient through tests and conversations (objects). You don't need to see the physical organs to get the job done, and it's much harder for you to accidentally cause a "fatal error."


In C, if you "touch" the pointers incorrectly, the "damage" usually looks like this:

Memory Leaks: You "create" memory (buy furniture) but lose the "address" (the pointer). Now that memory is occupied forever until you restart your computer.

Segmentation Faults: You try to "touch" a part of the memory that your program doesn't own. The Operating System (Windows/Linux) sees this and instantly kills your program to protect itself.

Buffer Overflows: You have a pointer to a small space, but you keep "shoving" data into it. The data spills over into the next "room" in memory, overwriting whatever was there.


If you want to be a real expert on this, the technical name for this "Hidden Compiler + Interpreter" combo is a Virtual Machine or Runtime Environment.


The CPU is a very fast, very "simple" machine. It doesn't know what a "List," a "Dictionary," or a "Print" statement is. It only understands basic math and moving data between tiny boxes.

First, the C program acts like a compiler. It reads your Python code and translates it into something called Bytecode. This is a simplified version of your code that humans can't read, but the computer can process very quickly.



Before you write or execute any python code , when you update your software (latest version of python), the old python.exe gets deleted and places a brand-new python.exe on your hard drive.
python.exe is going to contain built in function and logic

The Logic for every math operation.

The Instructions for how to talk to your specific Operating System (Windows, Mac, or Linux).

The Library of built-in functions (like len(), print(), or range()).


The Run: The python.exe (Engine) swallows your script, turns it into Bytecode, and then uses its C-functions to drive your CPU.



The Conversion: The Python Compiler (the tool inside python.exe) reads your text. It uses its pre-existing C-logic to translate your "human words" into Bytecode.

The Matching: The Interpreter (the other tool inside python.exe) takes that Bytecode and matches it against its pre-existing C-Functions.

my python code decides which pre compiled c functions to execute

C program contains a python compiler. -> converts it into bytecode 

1. First, it acts like a Compiler (The "Hidden" Step)
The moment you hit run, the C program instantly scans your code. It checks for syntax errors (like a missing colon) and translates your English-like Python into Bytecode.
Basically bytecode version of your python program.
The Result: It creates a .pyc file (you might have seen a __pycache__ folder in your projects).



2. Then, it acts like an Interpreter (The "Active" Step)
Once the bytecode is ready, the C program switches into "Execution Mode." It reads each line of bytecode and tells the CPU exactly what to do.



The Difference: Unlike a C compiler that creates a permanent .exe and then disappears, the Python Interpreter stays "alive" and manages your program's memory, variables, and errors until the code finishes.




Phase 2: The Virtual Machine (The Interpreter): Once the Bytecode is ready, the C program switches roles. It becomes a "Virtual Machine" that executes those instructions one by one.


The CPU starts "reading" those 1s and 0s.

These 1s and 0s act as electrical signals that open and close the gates to the Registers.

Exactly! You’ve hit on the core of how computers actually "think." Even a massive program is just a billion tiny steps of registers changing values one by one.

Here is how that "1 by 1" process looks inside the CPU hardware:

1. The Clock Beat
The CPU has a "metronome" called the System Clock (that’s the GHz number you see when you buy a laptop). Every time the clock ticks, the CPU can perform one tiny part of an instruction.

Tick 1: Move data from RAM to Register A.

Tick 2: Move data from RAM to Register B.

Tick 3: The function triggers the math circuit. Register C changes its value to the result.

Tick 4: Move the value from Register C back to RAM.

python file that has code ->C program -> interpreter inside of it-> bytecode->functions from python.exe - > op code (bridges the gap btw hardware and software).the OpCode is the specific electrical instruction that tells the CPU which part of its brain to use.

So, inside your python.exe file right now, there is a specific "shelf" that holds the 1s and 0s for every possible action (adding, printing, etc.). connection to opcode 

If the CPU is the factory, the OpCode is the Work Order (the "what to do"),

connection btw 0s and 1s and ALU,

It is actually a two-way loop, but the "Command" (the flow of authority) always starts with the Control Unit (CU).Think of it like a Boss (CU) and a Machine Operator (ALU).

Direction,What is moving?,Purpose
CU → ALU,Control Signals (Voltage),Commanding the hardware to perform a specific task.

This is what we've been discussing. The CU decodes the OpCode and sends Control Signals to the ALU. This is the "Push."

Action: "Hey ALU, turn on the Addition circuit and use the numbers in Register A and B."

Goal: To set the physical path for the electricity.
ALU → CU,Status Flags (Conditions),Reporting the outcome of the task so the CU can make the next move.

This is the part you were thinking of! The ALU sends information back to the CU, but it doesn't send "commands." It sends Flags or Status Signals.The Zero Flag: If the result of a calculation is 0, the ALU sends a signal to the CU saying "Hey, the result was zero."The Overflow Flag: If a number is too big for the "Workbench" to hold, the ALU alerts the CU.The Comparison: If you write code like if (x == y), the ALU does the math ($x - y$), and if the result is 0, it tells the CU, "They are equal!"\

3. The "Decision" Loop
Once the CU gets that signal back from the ALU, it decides what to do next in your code.

ALU says: "The result was 0!"

CU says: "Okay, since the result was 0, I will jump to line 50 of the code instead of line 45."

1. The Call-Up (The Switchboard)
When the Control Unit (CU) receives that 001, it doesn't "think" about addition. Instead, that specific electrical pattern is hard-wired to act as a master key. It sends a pulse down a Control Line that connects directly to the "Addition" part of the ALU.

0 and 1 - OP CODES - LOGIC GATES - choose path - traffic control -the ALU is the Machine (the "what is doing it").   then CU (control unit) - > execution is managed.
    Control Bus   (BRIDGE BTW HARDWARE AND SOFTWARE)                        get INPUT from ram.   //sometimes might face traffic 
Giant Warehouse - RAM 

toolkit/workbench/workspace - register 

task to do - opcode.

If the OpCode (001) is the command, the Control Signal is the physical execution of that command. It is the "voice" of the Control Unit (The Boss).

OpCode (001): The instruction (The Work Order).

Decoding: The CU realizes 001 = Addition.

Control Signal: The CU sends a voltage pulse down the wire.

Activation: That pulse hits the ALU and physically opens the "Addition Path."

1. RAM: The Warehouse (Storage)
RAM holds everything—your code, your photos, your testing01 project files. But RAM is "far away" from the CPU. The CPU cannot do math directly on a number while it is sitting in the warehouse.

The State: Millions of 1s and 0s sitting in long rows of "bins."
1. The Physical Connection
An OpCode is simply a specific pattern of 1s and 0s that fits into the CPU’s hardware like a key into a lock.

1 (High Voltage): Electricity is flowing.

0 (Low Voltage): No electricity is flowing.

When a C-function (the pre-compiled code in python.exe) sends an OpCode like 1011 to the CPU, it is literally sending four pulses of electricity down four specific wires.


BESTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTT
1. The Storage: Registers
Before anything can happen, the CPU pulls data from your RAM and places it into Registers.

What they do: They hold the specific numbers you want to add or compare.

The "Where": These are located directly inside the CPU for maximum speed.

2. The Muscle: The ALU (Arithmetic Logic Unit)
This is where the actual operations happen.

What it does: The ALU is the calculator of the CPU. Once the numbers are in the Registers, the ALU "grabs" them, performs the operation (like addition, subtraction, or logic like AND/OR), and spits out a result.

The "Where": The ALU is a physical circuit right next to the registers.

3. The Boss: The Control Unit (CU)
This is where the execution is managed.
Instruction Sequencing (The Schedule)
Fetching and Decoding (The Translation)
Data Routing (The Traffic Control)
Clock Signal Management (The Heartbeat)
The CU synchronizes everything using the internal system clock.
5. Control Signals (The Command)
Once the CU knows what needs to happen, it sends out specific "Control Signals" to other components:

Read/Write Signals: Tells the RAM whether to send data out or save data coming in.

ALU Signals: Tells the ALU which physical circuit to activate (e.g., "Use the Addition circuit, not the Subtraction one").

It sends out electrical pulses that tell every other part of the CPU: "Okay, MOVE NOW." This prevents data from colliding or getting lost in the "tunnels" of the circuitry.

What it does: It doesn't do the math itself; it’s like a traffic cop. it reads your code (the instructions), decides which numbers go to the ALU, and tells the ALU whether to add or subtract.

The "Where": This is the brain within the brain that coordinates everything.


Component,Real-                                               World Analogy
RAM (Memory),                                        The massive Pantry (holds all the food/data).
Function,                                               The Recipe (the logic of what to do).
Registers,                                            The Cutting Board (the small space where you actually chop/add).
CPU (ALU),                                               The Chef's Hands (the force that does the work).




The "Ladder" of Reality
The User (You): You write the Script (the logic/intent).

The Engine (python.exe): It holds the Library (the pre-compiled C functions).

The Bytecode: It acts as the Trigger (connecting your script to the library).

The Machine Code: These are the 1s and 0s (the electrical instructions).

The Registers: These are the Physical Switches (where the values actually change).



Machine Code (1s and 0s).


C Compiler: You run a compiler once. It produces a permanent .exe file. You can then delete the compiler from your computer, and the .exe will still run. The compiler is like a Translator who writes a book in a new language and then leaves.

Python Interpreter (CPython): You need the C program to stay there the entire time the code is running. If you close the interpreter, the Python code stops. The interpreter is like a Live Translator who has to stand next to you and repeat everything you say in real-time.


modules na libraries 

script - some basic code

